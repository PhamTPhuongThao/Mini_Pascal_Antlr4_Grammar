
        FUNCtion aaa(): real ;
        var x1,y2: real;
        begin
            with num: real ; arr : array [1 .. 2] of string ; do begin
                func(arr);
                proc(x, y, z);
                with i: real ; do begin
                    if(x>0) then merge(i,"test");
                end end end
        